name: Run Worker

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *' # Every hour

jobs:
  run-worker:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./core
        
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.26'
        cache-dependency-path: core/go.sum
        
    - name: Install dependencies
      run: go mod download
      
    - name: Run Worker
      env:
        DATABASE_URL: ${{ secrets.DATABASE_URL }}
        MONNIFY_CLIENT_ID: ${{ secrets.MONNIFY_CLIENT_ID }}
        MONNIFY_CLIENT_SECRET: ${{ secrets.MONNIFY_CLIENT_SECRET }}
        MONNIFY_ACCOUNT_ID: ${{ secrets.MONNIFY_ACCOUNT_ID }}
        MONNIFY_WEBHOOK_SECRET: ${{ secrets.MONNIFY_WEBHOOK_SECRET }}
        MONNIFY_BASE_URL: ${{ vars.MONNIFY_BASE_URL || 'https://api.monnify.com/v1' }}
      run: go run cmd/worker/main.go
